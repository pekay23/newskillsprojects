# Mosquitto 2.x Configuration
# Default listener on port 1883
listener 1883

# Allow anonymous connections (dev only — lock down in production)
allow_anonymous true

# WebSocket listener
listener 9001
protocol websockets
